{% extends "base.twig" %}

{% block content %}
<h1>Blog posts</h1>

{% if posts %}
<ul>
  {% for post in posts %}
    <li><a href="{{ post.url }}">{{ post.title }}</a></li>
  {% endfor %}
</ul>
{% endif %}
{% endblock %}
